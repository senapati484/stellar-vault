[package]
name = "stellar_vault"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "21.0.0"

[profile.release]
opt-level = "z"
lto = false
codegen-units = 1
overflow-checks = true